import React from 'react';
import { MainLayout } from '../../components';
import { Products } from '../../containers'

const Main = () => (
    <MainLayout>
        <Products />
    </MainLayout>
);

export default Main;
